import { RefObject } from 'react';
export interface UseSlideWidthProps {
    containerRef: RefObject<HTMLElement>;
    slidesToShow: number;
    gap: number;
}
export declare const useSlideWidth: ({ containerRef, slidesToShow, gap, }: UseSlideWidthProps) => number;
//# sourceMappingURL=useSlideWidth.d.ts.map